{
  "identifier": "default",
  "description": "Default capabilities for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "opener:allow-open-url",
    "updater:default",
    "process:allow-restart"
  ]
}
